# Examples

The [example server](example.mjs) demonstrates the template's runnable MCP
surface.

## Prerequisites

Use Node.js 26 and configure `MCP_TOKEN` only when testing authenticated HTTP
transport.

## Command

```bash
node examples/example.mjs
```

## Expected result

The example starts the documented local MCP workflow without exposing a live
endpoint or committing credentials.
