# Specifications

Repository authority: [authority.json](authority.json). Shared directive index:
[directives.json](directives.json).

This template applies the shared `general`, `application`, and `mcp-server`
profiles. Derived projects should add repository-specific structured
specifications here without copying the shared convention files.
